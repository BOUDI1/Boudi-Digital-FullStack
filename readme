🚀 Boudi Digital Platform - Backend Hybride
Boudi Digital est une application Full-Stack robuste développée pour une micro-entreprise de services digitaux. Ce projet valide les compétences des Activités 1 et 2 du titre professionnel DWWM.

L'innovation majeure de cette version réside dans son architecture de données hybride, utilisant à la fois du SQL (relationnel) et du NoSQL (orienté document) pour optimiser le traitement et le monitoring des données.

🛠️ Stack Technique
Front-end : HTML5 sémantique, CSS3 (Flexbox/Grid), JavaScript ES6+ (Fetch API).

Back-end : PHP 8.2 (Apache).

Bases de données :

MySQL 8.0 : Stockage structuré des contacts clients.

MongoDB : Logging asynchrone de l'activité utilisateur (NoSQL).

Infrastructure : Docker & Docker Compose pour l'orchestration des services.

Gestion des dépendances : Composer (SDK MongoDB).

✨ Fonctionnalités Clés
Interface Responsive : Design fluide adapté à tous les terminaux.

Formulaire de Contact Asynchrone : Envoi de données via l'API Fetch sans rechargement de page.

Double Persistance :

Enregistrement sécurisé dans MySQL via des requêtes préparées PDO.

Génération d'un log technique dans MongoDB pour le monitoring technique.

Environnement Isolé : Déploiement via Docker garantissant la portabilité du code.

🐳 Installation et Lancement (Docker)
1. Pré-requis
Docker Desktop installé.

Git installé.

2. Clonage et Démarrage
Bash
# Cloner le projet
git clone https://github.com/BOUDI1/Boudi-Digital-FullStack.git
cd Boudi-digital-FullStack

# Lancer les conteneurs (Build l'image PHP et lance les DB)
docker compose up -d --build

# Installer les dépendances PHP (Composer)
docker compose exec php-app composer install --ignore-platform-req=ext-mongodb
3. Accès
Application : http://localhost:8080.

MySQL (SQL) : Port 3306 (User: root / Pass: root).

MongoDB (NoSQL) : Port 27017.

📁 Structure du Projet
/api : Scripts PHP de traitement et connexion aux bases.

/images : Ressources visuelles et photo de profil.

Dockerfile : Configuration de l'environnement PHP personnalisé.

docker-compose.yml : Orchestration des 3 conteneurs (App, MySQL, Mongo).

📦 Installation en local
Clonage du dépôt :

Bash
git clone https://github.com/BOUDI1/Boudi-Digital-FullStack.git
Installation des dépendances :

Bash
npm install
Lancement du serveur :

Bash
npm start
Accès à l'application sur http://localhost:3000.